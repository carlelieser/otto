{
  "name": "otto-sqlite-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Validates the SQLite storage assumption (runtime.md §4) against a synthetic 5-year corpus.",
  "scripts": {
    "spike": "node src/run.js",
    "generate": "node src/generate.js",
    "verify": "node src/verify.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "sqlite-vec": "^0.1.7-alpha.2"
  }
}
